BACKEND

La base de datos se genera solo una vez se ejecuta el Back, con 2 perfiles por defecto (ADMIN y ALMACENERO), luego de ejecutar por 1er vez el back hay que insertar los datos.


FRONTEND

El Font ha tenido problema de conexión con el cors, es por ello que se le agrego un proxy, importante desplegar el proyecto front con el siguiente comando:

ng serve --proxy-config proxy.conf.json